import { ResponseTransformInterceptor } from './response-transform.interceptor';

describe('ResponseTransformInterceptorInterceptor', () => {
  it('should be defined', () => {
    expect(new ResponseTransformInterceptor()).toBeDefined();
  });
});
